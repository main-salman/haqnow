# Core API and database (ESSENTIAL)
fastapi==0.104.1
uvicorn[standard]==0.24.0
structlog>=23.1.0
pymysql>=1.1.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2  # Pin to 4.x for passlib compatibility
python-dotenv==1.0.0
pydantic==2.5.2
email-validator>=2.0.0
requests==2.31.0
redis>=5.0.0

# Storage
boto3>=1.35.0

# Document processing (lightweight)
Pillow==10.1.0
PyPDF2==3.0.1
pypdf==3.17.4
python-docx>=1.1.0
pandas>=2.0.0
numpy==1.26.4

# OCR - pytesseract only (uses system Tesseract, ~10KB)
pytesseract>=0.3.10
pdf2image>=1.16.3
reportlab>=4.0.0
# NOTE: Removed easyocr (~1GB) - not used, pytesseract handles all OCR
# NOTE: Removed opencv-python (~200MB) - not used, Pillow handles all image processing

# Authentication
pyotp==2.9.0
qrcode==7.4.2

# Email
sendgrid>=6.11.0

# Fuzzy string matching for typo-tolerant search
rapidfuzz>=3.0.0

# RAG/AI stack - Cloud LLM + Local embeddings
groq>=0.4.1
openai>=1.12.0

# NOTE: Removed spacy (~500MB) - not used anywhere in codebase
# NOTE: sentence-transformers moved to Dockerfile for CPU-only torch install
